{
  "name": "main",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/electron.js",
  "scripts": {
    "start": "run-p compile nodemon",
    "compile": "tsc src/electron.ts -w --outDir dist",
    "nodemon": "cross-env NODE_ENV=development nodemon --exec \"wait-on http://localhost:3000 && electron .\""
  },
  "license": "ISC",
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "@types/sharp": "^0.25.1",
    "cross-env": "^7.0.2",
    "nodemon": "^2.0.4",
    "npm-run-all": "^4.1.5",
    "typescript": "^3.9.7",
    "wait-on": "^5.1.0"
  },
  "dependencies": {
    "electron": "^9.1.2",
    "sharp": "^0.25.4",
    "slash": "^3.0.0"
  }
}
