{
  "name": "image-compressor",
  "version": "1.0.0",
  "description": "Application which compress and resize your images",
  "main": "dist/index.js",
  "scripts": {
    "start": "env NODE_ENV=development run-s main:build electron:watch",
    "main:build": "parcel build src/index.ts --target electron",
    "electron:watch": "run-p -r parcel:watch electron:start",
    "electron:start": "nodemon --watch dist/index.js --watch src/index.html --watch src/style.css  --exec \"electron .\"",
    "parcel:watch": "run-p -r main:watch renderer:watch",
    "main:watch": "parcel watch src/index.ts --target electron",
    "renderer:watch": "parcel watch src/index.html --public-url ."
  },
  "author": "Bartosz Dryl",
  "license": "MIT",
  "devDependencies": {
    "electron": "^9.0.0",
    "nodemon": "^2.0.4",
    "npm-run-all": "^4.1.5",
    "parcel": "^1.12.4",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.3"
  },
  "dependencies": {
    "tailwindcss": "^1.4.6"
  }
}
